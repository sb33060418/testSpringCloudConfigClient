 spring:
  cloud:
    config:
      name: config-client
      profile: dev
      #uri: http://localhost:8764/  
      label: master
      discovery: 
        enabled: true
        service-id: config-server